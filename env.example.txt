# Copy to .env and fill in (EmailJS dashboard: Email Services / Email Templates / Account).
# Only the PUBLIC key goes here. Never put the EmailJS private key in a VITE_* variable.
VITE_EMAILJS_PUBLIC_KEY=your_public_key
VITE_EMAILJS_SERVICE_ID=your_service_id
VITE_EMAILJS_TEMPLATE_ID=your_template_id